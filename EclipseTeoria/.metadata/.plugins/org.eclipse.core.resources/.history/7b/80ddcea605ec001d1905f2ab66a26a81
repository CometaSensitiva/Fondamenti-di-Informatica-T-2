package esempio6;

/**
 * 
 * The StudenteLavoratore interface represents a student worker and is a
 * combination of the Studente and Lavoratore interfaces. This interface is an
 * empty interface that defines the intersection type.
 */
public interface StudenteLavoratore extends Studente, Lavoratore {
// empty interface
}