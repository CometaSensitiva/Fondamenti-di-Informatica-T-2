package esempio3;

/**
 * This interface represents a right isosceles triangle, which is a triangle
 * that has two sides of equal length and one angle equal to 90 degrees.
 * 
 * Multiple inheritance is not supported in Java, so one must choose to derive
 * this interface from either {@link RightTriangle} or
 * {@link IsoscelesTriangle}, both of which this interface extends. Since the
 * operations defined by {@link RightTriangle} are more complex than those
 * defined by {@link IsoscelesTriangle}, this interface chooses to derive from
 * {@link RightTriangle}.
 * 
 * 
 * 
 * @author michelemazzaa
 * @see RightTriangle
 * @see IsoscelesTriangle
 */
public interface RightIsoscelesTriangle extends RightTriangle, IsoscelesTriangle {

	/**
	 * Returns a new instance of a right isosceles triangle with the specified side
	 * length.
	 * 
	 * @param side the length of the sides of the right isosceles triangle
	 * @return a new instance of a right isosceles triangle
	 */
	public static RightIsoscelesTriangle of(double side) {
		return new ImplRightIsoscelesTriangle(side);
	}
}